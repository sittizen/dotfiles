# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
font pango:DejaVu Sans Mono 8
focus_on_window_activation none

exec --no-startup-id xset s off
exec --no-startup-id nm-applet
exec --no-startup-id picom --config  ~/.config/i3/picom.conf -b

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# program launcher
bindsym $mod+d exec --no-startup-id dmenu_run

# most used apps
bindsym $mod+1 exec --no-startup-id run_or_focus "code ."
bindsym $mod+2 exec --no-startup-id run_or_focus "ghostty"
bindsym $mod+3 exec --no-startup-id run_or_focus "zen"
bindsym $mod+4 exec --no-startup-id run_or_focus "obsidian"
bindsym $mod+5 exec --no-startup-id run_or_focus "obs"
bindsym $mod+6 exec --no-startup-id run_or_focus "kdenlive"
bindsym $mod+8 exec --no-startup-id run_or_focus "flameshot gui"

# CONTAINERS
bindsym $mod+Shift+q kill
bindsym $mod+r mode "resize"
mode "resize" {
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym i resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt
        bindsym Escape mode "default"
}
# move focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+i focus up
bindsym $mod+l focus right
# move container
bindsym $mod+Control+j move left
bindsym $mod+Control+k move down
bindsym $mod+Control+i move up
bindsym $mod+Control+l move right
# toggle fullscreen
bindsym $mod+f fullscreen toggle
# change layout
bindsym $mod+t layout tabbed
bindsym $mod+e layout toggle split

# WORKSPACES
workspace 1 output HDMI-A-0
workspace 2 output HDMI-A-0
workspace 3 output HDMI-A-0
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
# switch
bindsym $mod+Shift+j workspace number $ws1
bindsym $mod+Shift+k workspace number $ws2
bindsym $mod+Shift+l workspace number $ws3
# move
bindsym $mod+Shift+u move container to workspace number $ws1
bindsym $mod+Shift+i move container to workspace number $ws2
bindsym $mod+Shift+o move container to workspace number $ws3

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
# status bar
bar {
        position top
        mode dock
        status_command i3status -c ~/.config/i3/i3status.conf
}
